
#logfile optional
#LOGFILE	compton.log

#start up type once= just start, keep= restart if dies
RUNTYPE	once

#command
COMMAND	compton -cC -r24 -o.6 -l-24 -t-22 --backend glx --glx-no-rebind-pixmap -z --glx-no-stencil
#COMMAND	compton -cC -r24 -o.6 -l-24 -t-22

#pidfile optional
#PIDFILE	compton.pid
